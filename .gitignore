# ----- 环境变量 / 机密（绝对不要提交）-----
.env
.env.*
!.env.example
*.pem
*.p12
*.key
*credentials*
*secret*
!*secret*.md
service_role*
.supabase/

# 本地密钥文件：优先用 config.example.js 作模板
# 注意：本仓库目前为了 Cloudflare/Pages 部署仍跟踪了 config.js（仅含 anon/publishable 公钥）。
# 千万不要把 service_role / sb_secret_ / 数据库密码写进任何会提交的文件。
# 若以后改为 CI 注入密钥，可取消下一行注释，并 git rm --cached config.js
# config.js

# ----- 本地分享 / 备份（太大或含私密内容）-----
馨宝与树宝-发给他打开.html
*备份*.json
找回-*.json

# 参考图 / 私密素材
assets/

# ----- 系统与构建产物 -----
.DS_Store
dist/
node_modules/
