# 本地分享用的整包文件（太大，网站不需要）
馨宝与树宝-发给他打开.html

# 本地导出的备份原件（已写入 seed-data.js，不必再上传）
*备份*.json

# 参考图 / 私密素材，不要上传
assets/

# 注意：config.js 里只有 publishable/anon 公钥（本就可放进网页）
# 千万不要把 service_role / secret 写进去

.DS_Store
dist/
